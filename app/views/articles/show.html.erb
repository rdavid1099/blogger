<h1> <%= @article.title %></h1>
<p><%= @article.body %></p>

<h3>Comments (<%= @article.comments.count %>)</h3>
<%= render partial: 'articles/comment', collection: @article.comments %>

<%= render partial: 'comments/form' %>
<br>
<%= link_to "Edit This Article", edit_article_path(@article.id) %> |
<%= link_to "Delete This Article", article_path(@article.id), method: :delete, data: {confirm: "Are you sure you want to delete this article?"} %> |
<%= link_to "<< Back to Main Page", articles_path %>
